<Window x:Class="CRUDPessoas.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CRUDPessoas"
        mc:Ignorable="d"
        Title="CRUDPessoas"
        Height="350"
        Width="600"
        Background="#121212"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>

        <!-- =============================== -->
        <!-- Estilo do Menu -->
        <!-- =============================== -->

        <Style x:Key="ModernMenu" TargetType="Menu">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <!-- MenuItem principal -->
        <Style x:Key="ModernMenuItem" TargetType="MenuItem">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="0,0,5,0"/>

            <Style.Triggers>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D2D30"/>
                </Trigger>

                <Trigger Property="IsSubmenuOpen" Value="True">
                    <Setter Property="Background" Value="#2D2D30"/>
                </Trigger>

            </Style.Triggers>
        </Style>

        <!-- Submenus -->
        <Style x:Key="ModernSubMenuItem"
               TargetType="MenuItem"
               BasedOn="{StaticResource ModernMenuItem}">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

        <!-- =============================== -->
        <!-- Botão moderno -->
        <!-- =============================== -->

        <Style x:Key="ModernButton" TargetType="Button">

            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="Button">

                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">

                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>

                        </Border>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

            <Style.Triggers>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C8CED"/>
                </Trigger>

                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>

            </Style.Triggers>

        </Style>

    </Window.Resources>


    <!-- ======================================= -->
    <!-- CARD PRINCIPAL -->
    <!-- ======================================= -->

    <Border Background="#1E1E1E"
            CornerRadius="12"
            Margin="25"
            BorderBrush="#2D2D30"
            BorderThickness="1">

        <Border.Effect>
            <DropShadowEffect
                BlurRadius="15"
                ShadowDepth="0"
                Color="Black"
                Opacity="0.4"/>
        </Border.Effect>


        <Grid Margin="30">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <!-- ================================= -->
            <!-- TÍTULO -->
            <!-- ================================= -->

            <StackPanel Grid.Row="0">

                <TextBlock Text="CRUDPessoas"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#FFFFFF"/>

                <TextBlock Text="Gerenciamento de pessoas"
                           FontSize="14"
                           Foreground="#A0A0A0"
                           Margin="0,5,0,25"/>

            </StackPanel>


            <!-- ================================= -->
            <!-- MENU -->
            <!-- ================================= -->

            <Border Grid.Row="1"
                    Background="#252526"
                    CornerRadius="8"
                    Padding="5"
                    BorderBrush="#3F3F46"
                    BorderThickness="1">

                <Menu Style="{StaticResource ModernMenu}">

                    <MenuItem Header="Pessoas"
                              Style="{StaticResource ModernMenuItem}">

                        <MenuItem x:Name="mniCadastrar"
                                  Header="Cadastrar"
                                  Style="{StaticResource ModernSubMenuItem}"
                                  Click="mniCadastrar_Click"/>

                        <MenuItem x:Name="mniPEE"
                                  Header="Pesquisar, editar e excluir"
                                  Style="{StaticResource ModernSubMenuItem}"
                                  Click="mniPEE_Click"/>

                    </MenuItem>

                </Menu>

            </Border>


            <!-- ================================= -->
            <!-- ÁREA PRINCIPAL -->
            <!-- ================================= -->

            <StackPanel Grid.Row="2"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center">

                <TextBlock Text="Bem-vindo!"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="#FFFFFF"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Utilize o menu Pessoas para começar."
                           FontSize="14"
                           Foreground="#A0A0A0"
                           Margin="0,8,0,0"
                           HorizontalAlignment="Center"/>

            </StackPanel>

        </Grid>

    </Border>

</Window>